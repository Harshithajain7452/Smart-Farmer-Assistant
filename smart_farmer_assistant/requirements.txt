# ---------------------------------------------------------------------
# Smart Farmer Assistant — Python dependencies
# Install with:  pip install -r requirements.txt
# ---------------------------------------------------------------------

# --- Web framework ---------------------------------------------------
Flask==3.1.3
Werkzeug==3.1.8
Jinja2==3.1.6
itsdangerous==2.2.0
click==8.1.8

# --- Extensions ------------------------------------------------------
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.2
WTForms==3.2.1
SQLAlchemy==2.0.44

# --- Database drivers ------------------------------------------------
PyMySQL==1.1.1            # MySQL driver (pure Python, no build step)
cryptography==44.0.0      # required by PyMySQL for caching_sha2_password

# --- Machine learning ------------------------------------------------
scikit-learn==1.9.0
numpy==2.2.6
pandas==2.2.3
joblib==1.4.2

# TensorFlow powers the two CNNs (disease + pest detection).
# It is intentionally optional: the app boots and every other module works
# without it, and services/ml_service.py degrades gracefully.
# Uncomment for full CNN inference (needs ~600 MB RAM):
# tensorflow-cpu==2.18.0

# --- Images & uploads ------------------------------------------------
Pillow==11.1.0

# --- External APIs ---------------------------------------------------
requests==2.32.3
python-dotenv==1.0.1
email-validator==2.2.0

# --- Production server ------------------------------------------------
gunicorn==23.0.0
